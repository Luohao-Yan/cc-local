/**
 * SnipTool prompt definitions
 *
 * HISTORY_SNIP feature: prompt text and tool name for the snip tool.
 */

export const SNIP_TOOL_NAME = 'snip'

export const SNIP_TOOL_PROMPT = `When the conversation context is getting long and you want to proactively manage context space, you can use the snip tool to request a conversation summary. This helps maintain performance by keeping the context window manageable.`

export const SNIP_BOUNDARY_MARKER = '__SNIP_BOUNDARY__'
