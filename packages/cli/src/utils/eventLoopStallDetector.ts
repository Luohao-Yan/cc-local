/**
 * Stub for internal-only event loop stall detector module.
 * This module is only imported when IS_INTERNAL_BUILD is true (it never is in external builds).
 */
export function startEventLoopStallDetector(): void {}
