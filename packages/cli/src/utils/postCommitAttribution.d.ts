/**
 * Type declarations for the postCommitAttribution module.
 * Internal Anthropic feature (COMMIT_ATTRIBUTION) that is disabled.
 */
export function installPrepareCommitMsgHook(
  repoPath: string,
  hooksDir?: string,
): Promise<void>
