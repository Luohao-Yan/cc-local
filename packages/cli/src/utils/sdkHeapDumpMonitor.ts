/**
 * Stub for internal-only SDK heap dump monitor module.
 * This module is only imported when IS_INTERNAL_BUILD is true (it never is in external builds).
 */
export function startSdkMemoryMonitor(): void {}
