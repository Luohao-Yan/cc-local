/**
 * Type declarations for the attributionTrailer module.
 * Internal Anthropic feature (COMMIT_ATTRIBUTION) that is disabled.
 */
export function buildPRTrailers(
  attributionData: unknown,
  attributionConfig: unknown,
): string[]
