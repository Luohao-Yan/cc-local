/**
 * Type declarations for the peerSessions module.
 * This is an internal Anthropic feature (UDS_INBOX) that is disabled.
 */
export function getSessions(): unknown[]
export function getSession(id: string): unknown | undefined
