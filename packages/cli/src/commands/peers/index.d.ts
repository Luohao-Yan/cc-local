/**
 * Type declarations for the peers command module.
 * This is an internal Anthropic feature (UDS_INBOX) that is disabled.
 */
import type { Command } from '../../types/command.js'

declare const peersCommand: Command
export default peersCommand
