/**
 * Type declarations for the skillSearch signals module.
 * This is an internal Anthropic feature (EXPERIMENTAL_SKILL_SEARCH) that is disabled.
 */
export type DiscoverySignal = 'aki' | 'native' | 'both'

export function getSignals(): unknown
