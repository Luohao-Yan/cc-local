/**
 * Type declarations for the remoteSkillLoader module.
 * This is an internal Anthropic feature (EXPERIMENTAL_SKILL_SEARCH) that is disabled.
 */
export function loadRemoteSkill(name: string): Promise<unknown>
export function prefetchRemoteSkills(): Promise<void>
